// Marketing has no standalone public router at launch.
// All provider-facing marketing routes are mounted under /dashboard/marketing
// via the dashboard router (Phase J). This file is reserved for future
// ops-level marketing management endpoints (post-launch Tier 1: campaign request UI).
//
// Launch scope: self-serve boost only, accessible via /dashboard/marketing/boosts.
// No open ad manager. No arbitrary promotion configuration (LOCKED).
export {};
